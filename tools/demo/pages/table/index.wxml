<view>
  <pro-table
    rowH="90"
    columns="{{ columns }}"
    sortField="{{ sortField }}"
    sortType="{{ sortType }}"
    dataSource="{{ dataSource }}"
    order="{{ orderOptions }}"
  />
</view>